import { StyleSheet } from "react-native";

export const searchBarStyles = StyleSheet.create({
  container: {
    padding: 10,
  },
  input: {
    backgroundColor: "#ddd",
    padding: 12,
    borderRadius: 10,
  },
});
